# Dependencies
node_modules/

# Secrets & credentials — JANGAN PERNAH commit ini
.env
credentials.json
token.json

# Session login WhatsApp (Baileys) — JANGAN commit
auth/

# Logs
*.log
logs/
npm-debug.log*

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code local settings (per-mesin)
.claude/settings.local.json

# Data keluarga (nomor HP -> panggilan) — PII, jangan di-commit
people.json

# Wrapper Windows Service (dibuat node-windows per-mesin, binary)
src/daemon/
